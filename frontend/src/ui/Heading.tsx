import styled from "styled-components";

const Heading = styled.h1`
  font-size: var(--font-size-xxs);
  font-weight: 600;
  text-transform: uppercase;
  color: var(--color-gar-700);
`;

export default Heading;
